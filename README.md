# LAYER

Perfume layering web app.

## Live URL (after Pages is on)
https://hasanazim499.github.io/layer-app/

Enable Pages: repo → Settings → Pages → Source = Deploy from a branch → `main` / `/ (root)` → Save.

Then bookmark that URL. After each update, hard-refresh (Cmd+Shift+R).
